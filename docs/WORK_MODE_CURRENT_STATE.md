# HazCom Navigator current Work state

Updated: 2026-09-26 07:02 America/Chicago. Resume point for the schema-2 development-cloud acceptance task.

- Canonical repository: `C:\Users\Me\Documents\coding projects\HAZCOM Navigator`, main `440b1635f00996ec7fc0b7ec6929850b319496d5` before this task's commit. The local `origin/main` tracking ref matched it; remote confirmation/push remains.
- Isolated writable worktree: `C:\Users\Me\.codex\.chatgpt-projects\g-p-6a82f26b189c8191a8d12e6bf588ffce\hazcom-publication-worktree`, detached from that SHA. The canonical checkout has the three source/test edits; this worktree also has the updated documentation and read-only cloud verification scripts. Copy approved docs/source to canonical main before commit. Preserve canonical untracked `Setup-HazComNavigator.ps1` and all frozen branches.
- Deployed backend source: `440b1635f00996ec7fc0b7ec6929850b319496d5`, 23 Functions, Firestore rules/indexes, private Storage rules in **only** `hazcom-navigator-dev`. This task changed no backend source, rules, or IAM roles; no Functions redeploy required. Cloud Run invocation settings were audited after the prior configuration change.

## Completed

- All 23 Cloud Run services were audited: `invokerIamDisabled=true`, no `allUsers` Run Invoker binding, zero IAM bindings, and reconciliation complete. All are callable/HTTPS client endpoints; `cleanupStagedPublication` requires Administrator Membership. Anonymous callable access reached application authentication and was rejected with 401. See [deployment handoff](FIREBASE_SCHEMA2_DEPLOYMENT_HANDOFF.md) for the service-by-service table.
- The four identical “Development Smoke Test” selector labels are four **distinct** active synthetic Company IDs (`smoke-19c`, `smoke-4aa`, `smoke-5e8`, `smoke-824`), each with a separate Manager Membership for the current Account. They are not duplicated access paths; no cloud records were deleted.
- Native Google session, Manager role, Pro entitlement, Company context and Windows SQLite authoring worked. Selected Company: `smoke-19c0423b-64d3-4138-9cf8-89fc162d1f54`. Its fixture has 2 Work Areas, 3 Chemical Products, 1 Worker, 2 Work Area Products, 2 Worker assignments, 3 SDS Verifications, 2 Training Events and 2 HAZCOM Reviews: 17 records and 3 SDS PDFs. The new Mixing Area links the synthetic Solvent Product and has a trained Worker.
- Fixed the native publication SQLite journal's closed-pool lifecycle by reusing one initialized connection and resetting the promise only on failed initialization. Multiple subsequent readiness and SDS operations had no lock/pool error. Temporarily renaming one hash-verified synthetic PDF made Reports show `Readiness: BLOCKING` with Publish disabled; restoring the exact bytes gave `READY`. Source also maps raw OS error 2 to a useful missing-SDS instruction.
- Native schema-2 publication committed revision 2 `c17fea81-8c4d-45e8-88ea-12ca4ea54c96` at `2026-09-26T11:41:10.584Z`. The first UI attempt showed a false immediate pointer-confirmation error even though the backend committed; subsequent readiness reconciled the journal and correctly matched the cloud. Source now retries the pointer read four times. An ordinary Work Area edit displayed “Local changes have not yet been published,” then a second native publication completed with its normal success message: current revision 3 `006f6e79-5646-403b-bf5c-715c8dcf8872` at `2026-09-26T11:54:29.162Z`. Both published revisions remain schema 2, complete 9/9 chunks, 17 records and 3 SDS, with validated manifest hashes; revision 3's parent is revision 2.
- Independent Firestore reads checked all eight entity collections and three verified attachment documents. A separate read-only development-cloud transport downloaded actual GCS SDS bytes, verified size/SHA-256, checked absence of `firebaseStorageDownloadTokens`, validated the manifest and imported revision 2 into a **fresh separate SQLite replica**. An injected corrupted revision-3 SDS download left revision 2 active; a clean retry atomically activated revision 3, and a repeat sync did nothing. This transport used privileged Firebase CLI OAuth, so it proves data/integrity/replication, not client Rules authorization.
- The signed-in native Firebase Storage SDK fetched one revision-2 SDS and verified 632 bytes plus SHA-256 `507dc54dbb5b38d0dae6505893f1faab1f869ab19edc5b888ba8972ff5187403`. Anonymous Firebase Storage and public GCS object URLs returned 403. Temporary native read instrumentation was removed.
- Validation: `npm run test:windows:publication` 6/6; `npm run test:firebase` 16/16; `npm test`; `npm run build`; Firebase Functions build; `HAZCOM_SCALE_SIZES=small npm run test:scale` including schema-2 security/recovery and 140-record/20-SDS publish to clean emulator replica. The 5,000-SDS stress test was not rerun.

## Remaining and exact next action

1. The updated handoffs and source/test files have been copied to canonical main. The worktree also has a generated synthetic PDF and read-only verification scripts; retain those as local acceptance artifacts. Do not commit user-specific CLI path, UID or hard-coded account information unnecessarily.
2. Review the canonical diff; commit the validated Windows journal/pointer fixes, focused test and documentation to main; push and confirm the remote SHA. Leave `Setup-HazComNavigator.ps1` untouched. Do not modify any frozen branch.
3. Remaining **live** security proof: authenticated nonmember SDS denial; Member and Demo trusted publication denial; direct privileged-field write denial. Emulator tests cover all these classes, but they are not live-cloud passes. A proposed real-cloud direct-write probe was rejected by automatic approval review because it could alter the Company pointer or Membership role. Do not route around that rejection. Obtain explicit approval for a specific safe test-identity/workflow before attempting such cloud mutations.

## Freeze and DO NOT CLAIM YET

Do **not** create `frozen/firebase-schema2-live-dev-2026-09-25` until all live acceptance gates, including the remaining security proof, pass. Do not claim live authenticated nonmember/Member/Demo denial or direct-write denial. Do not claim a final committed/pushed SHA until Git confirms it. Native publication, independent cloud data/replica integrity, signed-in SDS read, anonymous SDS denial and local-change detection are verified.
